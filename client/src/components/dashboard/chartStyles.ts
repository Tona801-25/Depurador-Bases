export const chartTooltipStyle = {
  backgroundColor: "hsl(var(--card) / 0.96)",
  border: "1px solid hsl(var(--primary) / 0.45)",
  borderRadius: "12px",
  boxShadow:
    "0 12px 36px hsl(var(--background) / 0.55), 0 0 22px hsl(var(--primary) / 0.18)",
  color: "hsl(var(--foreground))",
  fontSize: "12px",
  fontWeight: 500,
};

export const chartTooltipCursor = {
  fill: "hsl(var(--primary) / 0.035)",
  radius: 6,
};